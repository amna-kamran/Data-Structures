#include <iostream>
using namespace std;

class adminClass{
	private:
		string username="admin";
		string password="admin";

	//methods accessible only by the admin
	//1)delete a client
	//2)view client details
	//3)check using the client's name and package number if a package has been delivered
	//4)can add a new destination at which a package can be delivered
	//5)can remove a destination
	
	void deleteClient(){
		
	}
}